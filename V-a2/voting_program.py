from voting_lib import Voting

if __name__ == '__main__':
    voting = Voting()
    # voting.single_printing_function(1)
    voting.main_option()
    # voting.printingAllData()
    # for i in range(len(data)):
    #     print(data[i])